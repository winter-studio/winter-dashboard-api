<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.winterframework.dashboard.base.mapper.MenuMapper">
    <update id="updateSortToTheEnd">
        UPDATE menu t
        SET t.sort = (SELECT ifnull(MAX(sort), 0) + 1 AS new_sort FROM menu b
        <choose>
            <when test="parentId == null">
                WHERE b.parent_id IS NULL
            </when>
            <otherwise>
                WHERE b.parent_id = #{parentId}
            </otherwise>
        </choose>
        )
    </update>

    <select id="getMenusByUserId" resultType="org.winterframework.dashboard.base.entity.Menu">
        SELECT *
        FROM menu
        WHERE id IN (SELECT menu_id
                     FROM role_menu
                     WHERE role_id IN (SELECT role_id FROM user_role WHERE user_id = #{userId}))
    </select>
</mapper>
