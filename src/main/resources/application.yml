spring:
  profiles:
    default: dev
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher #开发环境打开，兼容springfox
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:ankenail}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379
